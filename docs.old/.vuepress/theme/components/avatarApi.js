import { AvatarStyleCount } from './avatar'

const config = Object.assign(AvatarStyleCount, {
  flip: 0,
  color: 'rgba(255, 0, 0, 0)',
  shape: 'none',
})

Object.keys(config).map((type) => {
  return ``
})


// const configBtoa = btoa(JSON.stringify(config))
